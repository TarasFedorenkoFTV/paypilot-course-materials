# Встановлення: від порожньої машини до робочого стенду

Покроково, з нуля. Розраховано на людину, яка раніше не піднімала в себе
нічого подібного. Приблизний час — **30–40 хвилин**, більшість із них
займає завантаження.

Наприкінці у вас у браузері відкриється чат зі стендом і відповість на
запитання.

Кроки йдуть у тому порядку, у якому їх треба робити. Найчастіша поламка на
цьому шляху — запустити стенд раніше, ніж вписаний ключ, тож не міняйте
місцями кроки 3 і 4.

---

## 0. Що знадобиться

| Що | Навіщо |
|---|---|
| **Ключ провайдера** | без нього стенд відповідає заглушками, і завдання на ньому не робляться |
| **Docker Desktop** *або* **Python 3.12+** | одне з двох, не обидва |
| Браузер | інтерфейс стенду |

Вільне місце на диску: плануйте **близько 6 ГБ** для шляху з Docker і
**близько 300 МБ** для шляху з Python.

**Реєструватися в Docker не потрібно.** Docker Desktop при першому запуску
пропонує створити акаунт і увійти — це вікно можна закрити. Стенд працює без
входу; акаунт потрібен лише тим, хто публікує власні образи.

---

## 1. Отримати ключ

**Ключ вам видає курс.** Не купуйте його самі й не заводьте особистий платний
акаунт: на кожного студента заводиться окремий ключ із власним лімітом витрат.

Ключ — довгий рядок, що починається з `sk-ant-`. Він показується **один раз**,
тому збережіть його одразу, щойно отримаєте.

Якщо ключа ще немає, решту інструкції можна пройти наперед: стенд підніметься
й без нього. Тільки пам'ятайте, що без ключа він відповідає заготовками —
однаково щоразу. Це годиться, щоб переконатися, що середовище зібране, і не
годиться для навчальних завдань.

---

## 2. Забрати репозиторій

Адресу репозиторію вам дає курс. Два способи, обидва прийнятні.

**Через git** — зручніше, якщо далі будуть оновлення. Потрібен встановлений
git ([git-scm.com/downloads](https://git-scm.com/downloads), під час
встановлення всі параметри можна лишити за замовчуванням):

```bash
git clone <адреса-репозиторію>
```

**Архівом, без git:** на сторінці репозиторію зелена кнопка **Code** →
**Download ZIP**, далі розпакувати.

У будь-якому разі розміщуйте теку так, щоб у шляху не було пробілів і
кирилиці: `C:\paypilot-stand` підійде, `C:\Users\Іван\Мої документи\стенд` —
ні.

Усі команди далі виконуються **з кореня цієї теки** — там, де лежать
`Dockerfile` і `requirements.txt`. У Windows найпростіше так: відкрийте теку в
провіднику, клацніть на адресний рядок, наберіть `powershell` і натисніть
Enter.

---

## 3. Вписати ключ — до першого запуску

У корені лежить файл **`.env.example`**. Зробіть із нього копію з назвою
**`.env`**:

```bash
cp .env.example .env
```

У PowerShell: `Copy-Item .env.example .env`

Відкрийте `.env` у будь-якому текстовому редакторі й приведіть два рядки до
такого вигляду:

```
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...ваш ключ...
```

Три речі, на яких спотикаються найчастіше:

- Файл має називатися рівно **`.env`**, а не `.env.txt`. Windows ховає
  розширення — увімкніть у провіднику «Розширення імен файлів» і перевірте.
- **Без пробілів** навколо `=` і без лапок навколо ключа.
- Якщо стенд уже запущений, після зміни `.env` його треба **перезапустити**:
  значення читаються під час старту.

Саме тому цей крок стоїть перед запуском. Стенд, піднятий без `.env`,
стартує успішно й мовчки працює на заглушках — жодної помилки ви не
побачите, а відповіді будуть несправжні.

**Ключ нікуди не комітиться.** `.env` уже прописаний у `.gitignore` — так і
лишіть. Не надсилайте ключ у чат групи й не вставляйте в домашні завдання.

---

## 4. Підняти стенд

Виберіть **один** зі шляхів. Docker простіший і дає повний набір
інструментів; Python легший для машини.

### Шлях А — Docker

1. Завантажте **Docker Desktop**:
   [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/).
2. Встановіть. На Windows інсталятор сам увімкне **WSL2** і, найімовірніше,
   попросить **перезавантажити комп'ютер** — перезавантажте.
3. Запустіть Docker Desktop і дочекайтесь, доки значок кита перестане
   блимати. Пропозицію увійти в акаунт закрийте.
4. У корені репозиторію:

```bash
docker compose up --build -d
```

Перший запуск триває **5–10 хвилин**: збирається образ. Наступні — секунди.

Чат відкриється на **http://localhost:8000**, UI трасування — на
**http://localhost:6006**.

Зупинити: `docker compose down`. Дані при цьому лишаються.

### Шлях Б — Python без Docker

Встановіть **Python 3.12 або новіший**
([python.org/downloads](https://www.python.org/downloads/)). На першому екрані
інсталятора обов'язково поставте галочку **«Add python.exe to PATH»** — без
неї команди нижче не знайдуться.

```bash
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
.venv/Scripts/python -m uvicorn app.main:app --port 8000
```

На Linux і macOS шляхи інші: `.venv/bin/pip` і `.venv/bin/python`.

Чат — на **http://localhost:8000**.

Зверніть увагу на `.venv/Scripts/python` замість просто `python`. Створення
venv не робить його активним: наступний `python` у тій самій консолі — це
системний інтерпретатор, у якому залежностей немає, і ви побачите
`No module named 'yaml'`. Або пишіть шлях повністю, як вище, або активуйте
venv один раз — `.venv\Scripts\activate` у PowerShell, `source
.venv/bin/activate` у Linux і macOS — і тоді працює короткий `python`.

У цьому шляху **порт 6006 буде мертвий**: UI трасування піднімається лише
через docker compose. Це нормально, і діагностика про це попередить. Трейси
однаково доступні — через `GET /api/_test/traces/...` і з файлу
`traces/traces.jsonl`. Жодне завдання курсу через це не блокується.

---

## 5. Перевірити, що все зібралося

Перша перевірка — діагностика:

```bash
.venv/Scripts/python scripts/doctor.py
```

У шляху з Docker: `docker compose exec stand python scripts/doctor.py`

| Рядок | Норма |
|---|---|
| `python` | `[OK]` з версією 3.12+ |
| `dependency: ...` | усі `[OK]` |
| `llm provider` | `[OK] anthropic`. Якщо `mock` — ключ не підхопився, поверніться до кроку 3 |
| `anthropic api reachable` | `[OK] HTTP 200` |
| `phoenix (trace UI)` | `[OK]` у Docker, `[WARN]` у шляху з Python — **обидва нормальні** |
| решта | `[OK]` |

Підсумок `Usable with warnings` при єдиному `WARN` про Phoenix — це робочий
стан, а не помилка.

Друга перевірка, вона ж найпереконливіша: відкрийте
**http://localhost:8000**, напишіть у чат

```
I am CUS-0001. What is my balance?
```

і дочекайтесь відповіді. Прийшла — стенд живий.

Якщо ви ще без ключа, відповідь виглядатиме дивно: замість речення ви
побачите сирий результат виклику інструмента, щось на кшталт
`Result of get_account: {...}`. Це не поламка, це заглушка. З ключем
відповідь буде нормальним текстом.

---

## 6. Що робити далі

Відкрийте **`docs/first-run.md`**. Це п'ятнадцятихвилинна проходка по циклу
роботи: звідки береться еталон, як побачити розбіжність і чому правильна на
вигляд відповідь сама по собі нічого не доводить. Починайте з неї, а не з
першого завдання.

---

## Якщо щось не працює

**`python` не знайдено.** Не поставлена галочка «Add python.exe to PATH» під
час встановлення. Запустіть інсталятор ще раз, оберіть *Modify* і поставте її.

**`No module named 'yaml'`** (або `fastapi`, або `uvicorn`). Ви запустили
системний python замість venv — див. пояснення в кроці 4Б.

**Діагностика каже `provider: mock`, хоча ключ вписаний.** Три причини за
частотою: файл названо `.env.txt`; пробіли навколо `=`; стенд не
перезапущений після редагування. Рідша четверта: змінна середовища, виставлена
в самій консолі, перебиває значення з `.env`.

**`HTTP 400` на кожен запит.** Найчастіше — вичерпаний ліміт витрат на ключі.
Напишіть куратору курсу; `doctor` покаже повідомлення провайдера зрозумілою
мовою.

**Порт 8000 зайнятий.** Щось уже слухає цей порт. Або зупиніть його, або
підніміть стенд на іншому: додайте `--port 8010` до команди uvicorn і
відкривайте `http://localhost:8010`.

**Docker Desktop не стартує на Windows.** Майже завжди — вимкнена
віртуалізація в BIOS або не встановлений WSL2. Docker Desktop зазвичай сам
показує посилання на потрібну інструкцію; якщо ні — виконайте `wsl --install`
у PowerShell від адміністратора й перезавантажтесь.

**Дані виглядають зіпсованими, стенд поводиться дивно.** У панелі праворуч є
кнопка **«Скинути стан»** — вона повертає базу до вихідного вигляду.

**Хочу переконатися, що стенд цілий.**
`.venv/Scripts/python scripts/ci_smoke.py` — перевіряє всі поверхні, ключ для
цього не потрібен.

Якщо нічого з переліченого не допомогло — надішліть куратору **повний вивід
`doctor`**. У ньому видно майже все, що може бути не так.
