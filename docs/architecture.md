# PayPilot — архітектурний опис

## Компоненти й потоки даних

```
                        ┌─────────────────────────────┐
   GET  /       ──────► │  app/main.py (FastAPI)       │  chat UI (static)
   POST /chat   ──────► │   chat surface + test API    │
   /api/_test/* ──────► │   + /compare (clean vs prof) │
                        └───────────────┬──────────────┘
                                        │
                            ┌───────────▼───────────┐
                            │ app/agent/loop.py      │  власний цикл оркестрації
                            │  model → tool → model  │  (D14 retry, D15 inflation)
                            └───┬──────────┬─────────┘
             prompt.build()     │          │   dispatch(name, args)
            (base.v1 + overlays)│          │
                  ┌─────────────▼──┐   ┌───▼───────────────┐
                  │ providers/*    │   │ app/agent/tools.py│  дефектні гачки
                  │ mock/anthropic │   │  9 інструментів    │  D09–D11,D19,D20,D22,D26
                  │ /openai        │   └───┬───────────────┘
                  └────────────────┘       │ виклики
                          ┌────────────────┼───────────────┬──────────────┐
                          ▼                ▼                ▼              ▼
                   app/engines/*    app/db.py        app/rag/         app/clock.py
                   fx/limits/       SQLite state     retriever        CLOCK_OVERRIDE
                   disputes         (reset→seed)     kb_clean/broken
                   (оракул)
                          │
                    app/tracing.py  ── дерево спанів → traces/traces.jsonl + API
```

## Ключові рішення

- **Власний цикл оркестрації** (не фреймворк) — вимога ТЗ §4.2. Цикл
  `model → tool → model` явний у [loop.py](../app/agent/loop.py), бо D14 і D15
  живуть саме в ньому.
- **Рушії як оракул** ([engines](../app/engines)) — детермінований код, що дає
  правильну відповідь. Дефект = розбіжність відповіді агента з рушієм. Рушії
  імпортуються з коду (студент отримує еталон програмно; ДЗ11 пише на них pytest).
- **Дефект-гачки в агентному шарі, не в рушіях.** Інструмент може викривити
  результат рушія (D19, D20), але сам рушій лишається правильним — інакше
  зникає оракул.
- **Провайдер-адаптер** ([providers](../app/agent/providers)) — нейтральний
  внутрішній формат повідомлень; mock (без ключа, детермінований), Anthropic,
  OpenAI. Перемикається змінною `LLM_PROVIDER`.
- **Профілі й overlay** — дефекти рівня специфікації вмикаються накладанням
  фрагментів на `prompts/base.v1.md` без правки базового файлу.
- **Трасування** — власний in-process трейсер (стабільний JSONL + API) плюс
  дзеркалювання в Phoenix через OTLP. API і файл працюють завжди, навіть коли
  колектор не піднятий: assertions студента не залежать від зовнішнього сервісу.
- **Чат-UI** — один статичний файл без збірки (`app/static/index.html`):
  перемикач профілів, точкове вмикання дефектів, керування часом, скидання
  стану, дерево спанів останнього ходу і режим «clean vs профіль».

## Життєвий цикл запиту `/chat`

1. `loop.run_turn` бере/створює сесію, будує system prompt (`base.v1` + активні
   overlay), відкриває кореневий спан `agent.request`.
2. Якщо перевищено `SUMMARIZE_AFTER_STEPS` — згортка історії (спан
   `agent.summarize`; D06 тут губить числа).
3. Цикл: `llm.call` → якщо є tool_calls, кожен виконується (`tool.<name>`,
   з аргументами й результатом в атрибутах; D14 — до 8 повторів на порожньому
   результаті) → результат кладеться в контекст (D15 роздуває) → повтор.
4. Фінальна відповідь без tool_calls завершує хід; дерево спанів пишеться
   в памʼять і `traces/traces.jsonl`.

## Порти й запуск

- Чат-UI + API: `http://localhost:8000` (docker compose або `make dev`).
- Phoenix (UI трасування): `http://localhost:6006`.
- Трейси: `GET /api/_test/traces/{request_id}` та файл `traces/traces.jsonl`.
- Стан: `GET /api/_test/state/{table}`.
