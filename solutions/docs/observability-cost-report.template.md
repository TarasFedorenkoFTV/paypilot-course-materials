# Observability & Cost Report — `<ваша система>`

Три слайди. Усі цифри — із записаних прогонів, не з оцінок. Модель: `<...>`,
прайс: `<...>`.

---

## Слайд 1 — де і за скільки

### Один сценарій, розкладений за трейсом

Сценарій: `<дослівний запит>` · чому саме він: `<...>`

| Крок | Спан | Вхідні | Вихідні | Час |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| **Разом** | | | | |

Той самий сценарій на дефектному профілі:

| Профіль | Вхідні | Вихідні | Ціна | Час |
|---|---|---|---|---|
| `clean` | | | | |
| `<профіль>` | | | | |
| **Співвідношення** | | | | |

> Токени й час **окремо**. Вони ростуть різними множниками, і за грошима
> часто видно менше, ніж за часом, якому клієнт чекає.

### Виділений спан

Назвіть **один** спан, де система заплатила за нічого: `<ім'я, атрибути>`

Речення для непрофільного читача, без слів «модель помилилася»:

> `<...>`

### Стан після прогону

Скільки рядків змінилося в базі: `<...>`

> Якщо нуль — дефект платить виключно латентністю й токенами, і жоден набір,
> що дивиться на відповідь або на стан, його не побачить. Це варто сказати
> прямо.

---

## Слайд 2 — прогноз

### Поверх 1: припущення окремо від замірів

| Параметр | Значення | Джерело |
|---|---|---|
| `<...>` | | **припущення** / **заміряно** |

> Розводьте їх навмисно. Припущення треба перевіряти на реальному трафіку,
> заміри — ні.

### Поверх 2: сума

```
<розрахунок рядок за рядком, щоб його можна було перевірити>
```

### Поверх 3: формула для N і що росте швидше за N

```
cost(N) = <...>
```

Що росте швидше за N і чому: `<...>`

> Майже завжди це не кількість користувачів, а довжина діалогу: вхідні токени
> на репліку не сталі, контекст накопичується.

---

## Слайд 3 — рішення

### Важелі, кожен із ціною

| Важіль | Ефект | **Що псується** |
|---|---|---|
| | | |
| | | |
| | | |

> Колонка «що псується» — головна. Важіль без ціни або не працює, або ви її
> не знайшли.

### Що обміняли · чого не робимо · за яких умов зміниться

**Що обміняли:** `<...>`
**Чого не робимо і чому:** `<...>`
**За яких умов рішення зміниться:** `<конкретні пороги, не «якщо стане гірше»>`

### Бюджети, записані в кейси

| Кейс | Обмеження | Що ловить |
|---|---|---|
| | | |

> Бюджет як **вимога**, а не спостереження: регресія вартості має блокувати
> мердж так само, як регресія коректності.
