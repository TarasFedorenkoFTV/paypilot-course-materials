# Specification Review — `<файл промпту>`

Аудит специфікації. Профіль прогонів: `<профіль>`. Дата: `<дата>`.
Артефакти аудиту: `<промпт>`, `<user story>`.

---

## 1. Карта анатомії

Позначка ставиться **не за наявністю заголовка**, а за тим, чи можна з блоку
вивести перевірку.

| # | Блок | Стан | Рядки, що його утворюють |
|---|---|---|---|
| 1 | Роль і тон | `<є / слабкий / порожній>` | `<цитата або номери рядків>` |
| 2 | Скоуп | | |
| 3 | Джерела правди | | |
| 4 | Правила інструментів | | |
| 5 | Доменні обмеження | | |
| 6 | Крайні випадки | | |
| 7 | Формат виходу | | |
| 8 | Приклади | | |

**Порожні блоки окремим списком:** `<...>`
**Слабкі:** `<...>`

> Порожній блок не має сліду в поведінці, поки хтось не спитає те, на що
> потрібен приклад. Саме тому карта складається читанням, а не прогонами.

## 2. Знахідки

Типи дефектів специфікації: суперечність · неверифікованість · неповнота ·
неоднозначність · невідстежуваність. Покрити щонайменше чотири з п'яти.

| # | Знахідка | Блок | Тип | Доказ | Severity (через бізнес-вплив) |
|---|---|---|---|---|---|
| F-01 | | | | | |
| F-02 | | | | | |
| F-03 | | | | | |

> Колонка **severity обґрунтовується наслідком**, не відчуттям: що станеться з
> клієнтом, з грошима, з регулятором. «Високий, бо важливо» — порожній рядок.

## 3. Доказовість однієї знахідки

Оберіть одну — найкраще суперечність — і доведіть **розподілом**, не вердиктом.

Питання: `<дослівний запит>`
Профіль: `<...>` · прогонів: `<N>`

| Прогін | `<що спостерігали>` | `<що спостерігали>` | Що зробив натомість |
|---|---|---|---|
| 1 | | | |
| 2 | | | |

**Розподіл:** `<напр. у 5 з 5 не назвав ціни, у 1 з 5 порушив другу заборону>`

Дослівні цитати щонайменше двох прогонів:

> `<цитата>`

> **Пишіть розподіл, а не вердикт.** «Агент помиляється» — не доказ. «У 4 з 5
> прогонів…» — доказ.

## 4. Переформульовані вимоги

Для 2–3 знахідок. Кожна вимога має **всі три частини**, інакше вона не
верифікована.

### R-1 — замінює F-`<...>`

> **Було:** `<цитата з промпту>`
>
> **Стало:** `<нове формулювання>`

- **тригер:** `<коли перевіряти>`
- **спостережуваний вихід:** `<що саме дивитися — текст, аргументи, спан, рядок БД>`
- **критерій:** `<як відрізнити пас від фейлу, з рівнем драбини>`

> Якщо на вимогу не можна написати перевірку — вона не йде в промпт. Її місце
> в гайді для копірайтера, і це нормальний результат аудиту.

## 5. Межі аудиту

Чого цей метод **не** знаходить:

1. `<...>`
2. `<...>`
3. `<...>`

> Мінімум один пункт має бути про дефекти, яких у промпті немає взагалі.
> Читання специфікації їх не дає — потрібен оракул.
