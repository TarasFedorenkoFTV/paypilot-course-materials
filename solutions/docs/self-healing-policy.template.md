# Self-Healing Policy — <ваша система>

Шаблон. Десять рядків; шкала автономії та сама, що для незворотних
інструментів: **автономно / з підтвердженням / ніколи**.

Порожній рядок — це «рішення не ухвалене», і воно видно на ревʼю.

---

## 1. Мандат
Діє: `<Ship it | Don't break it>`. Що змінилося б за протилежного:
`<назвіть рядки цього документа, які змінилися б>`.

## 2. Що агент робить у пайплайні
| Режим | Де | Права |
|---|---|---|
| | | |

> Права токена вкажіть явно. `contents: write` для рецензента — це вже не
> рецензент.

## 3. Автономно — без підтвердження
`<умова, за якої правка застосовується сама>`
Виміряна поверхня: `<скільки з N білдів>`.

## 4. З підтвердженням людини
- `<клас>` → `<хто підтверджує>`

## 5. Ніколи
| Шлях / дія | Чому |
|---|---|
| | |

> Заборона мусить бути в **коді**. Перевірка: чи спрацює вона, якщо модель
> проігнорує промпт?

## 6. Що вимірюється і як часто
| Цифра | Значення | Інтервал | Коли перемірюється |
|---|---|---|---|
| | | | |

> Цифра без інтервалу на малій вибірці — не результат.

## 7. Хто дозволив
Рівень шкали, **не ім'я людини**: `<рівень>`. Розширення автономії:
`<рівень + умова>`.

## 8. Слід і відкат
Де лежить пропозиція: `<шлях>`. Як відкотити: `<одна команда>`.

## 9. Що агент не вирішує ніколи
- `<рішення, яке лишається за людиною>`

## 10. Чому попередня версія superseded — і за яких умов зміниться ця
`<що зламав замір і що з цим зроблено>`
Ціна виправлення: `<що стало дорожче або незручніше>`.

Ця версія зміниться, коли:
- `<тригер>` → `<що саме>`

Версія: `<vN>`, `<дата>`. Попередня: `<vN-1, superseded, причина>`.
