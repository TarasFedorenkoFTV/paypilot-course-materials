# Security & Red Teaming Log — `<ваша система>`

Журнал знахідок. Кожна знахідка має **вхід**, через який прийшла, — це
визначає, чим вона лікується.

Входи: `user_turn` · `tool_result` · `history` · `retrieved_context` · ланцюжок

---

## Знахідки

### S-01 · `<назва>` — вхід `<...>`

**Спроба (дослівно):**
```
<точний текст, який надсилали>
```

**Результат:** `<що зробив агент — дослівна цитата>`

**Відтворюваність:** `<N з M прогонів>`

**Що саме витекло / що виконалося:** `<...>`

**Бізнес-вплив:** `<наслідок, не «критично»>`

**Клас виправлення:** `<промпт / інструмент / архітектура / не лікується>`

**Перевірка, що це ловить:** `<id кейсу, рівень драбини, на що дивиться>`

---

### S-02 · `<назва>` — вхід `<...>`

`<той самий блок>`

> Мінімум чотири знахідки, і **щонайменше дві з різних входів**. Чотири
> варіації одного `user_turn` — це одна знахідка, записана чотири рази.
>
> Окремо цінна знахідка — **спроба, яка не спрацювала**. Вкажіть її теж: те,
> що модель тримає межу, це результат, а не порожнеча.

---

## Залишковий ризик

`<що лишається після виправлень і чому це прийнято>`

## Пріоритет за бізнес-впливом — і чим він відрізняється від пріоритету за ефектністю

| Знахідка | За ефектністю | За бізнес-впливом | Чому різниця |
|---|---|---|---|
| | | | |

> Найвидовищніша знахідка рідко найдорожча. Ця таблиця — головна частина
> документа для того, хто ухвалює рішення.

## Розкладка по шарах гейта

| Знахідка | Шар | Чому саме там |
|---|---|---|
| | блокуючий / nightly / release | |

> Критерій — детермінованість і ціна, не важливість. Гейт, який червоніє
> випадково, навчає команду його обходити.
