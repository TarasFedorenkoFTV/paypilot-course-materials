# AI Adoption Impact Report — <ваша система>

Три слайди, сім рядків. Список мутантів (або інший незмінний вимірник)
фіксується **до** першого прогону й однаковий для всіх наборів.

---

## Слайд 1 — що заміряно

| Рядок | Ручний | Згенерований |
|---|---|---|
| 1. Тестів | | |
| 2. Хвилини людини | | `<ревʼю окремим рядком>` |
| 3. Хвилини машини | | |
| 4. Mutation score | | |
| 5. Прийнято після ревʼю | — | `<прийнято / відхилено / переписано>` |
| 6. Знайдених справжніх дефектів | | |
| 7. Час на один убитий мутант | | |

> Рядок 2 має показувати **хвилини ревʼю окремо**. Генерація за дві хвилини
> не економить час письма — вона переносить його на читання.

## Слайд 2 — розклад по класах

| Клас | Мутантів | Ручний | Згенерований |
|---|---|---|---|
| | | | |

> Чотири рядки мінімум, не лише підсумок. Одна цифра ховає, до якого класу
> дефектів набір слiпий.

## Слайд 3 — рішення

### Роль асистента, знайдена виміром
`<що він робить добре — за цифрами, не за відчуттям>`

### Не роль асистента
`<де він дає нуль і чому це не виправляється кількістю>`

### Де не працює
`<структурна причина, не «модель слабка»>`

### Рядок мандату
Діє `<мандат>`. За протилежного рішення було б `<яким і чому>`.

### За яких умов рішення зміниться
- `<тригер>` → `<що саме>`

---

## Декларація використання AI

**Що робив асистент:** `<...>`
**Що робив я:** `<...>`
**Що відхилено з виводу:** `<скільки і чому — порядково в журналі ревʼю>`
**Помилка як допустима здача:** `<якщо асистент програв — так і напишіть>`
