# RAGAS Diagnostic Report — `<ваша система>`

Мета — не порахувати метрики, а **локалізувати**: збій у пошуку чи в
генерації. Метрика без локалізації нічого не вирішує.

---

## 1. Контекст

Набір: `<назва, скільки кейсів, скільки з них мають reference>`
Індекс: `<...>` · `k`: `<...>` · модель: `<...>`

## 2. Локалізація: симптом → точка відмови → доказ

| Симптом | Точка відмови | Доказ |
|---|---|---|
| `<що бачить користувач>` | пошук / генерація | `<метрика + число, або цитата фрагмента>` |
| | | |
| | | |

> «Точка відмови» має бути **одна** на рядок. Якщо не можете обрати — доказу
> не вистачає.

## 3. Доказ причини: пара прогонів на одному наборі

Той самий набір, змінюється **одне**: `<що саме>`

| | Прогін A | Прогін B |
|---|---|---|
| що змінили | | |
| context recall | | |
| context precision | | |
| faithfulness | | |
| що це доводить | | |

> Дві змінні одночасно — і прогін нічого не доводить. Міняйте одне.

## 4. Зворотний випадок: контекст цілий, відповідь хибна

Кейс: `<id>` · потрібний фрагмент у контексті: `<так>` · відповідь: `<хибна>`

Що це доводить: `<...>`

> Без цього розділу звіт читається як «винен пошук». Зворотний випадок
> показує, що генерація ламається окремо, і лікується вона інакше.

## 5. Компроміс: recall проти precision на трьох значеннях k

| k | recall | precision | вхідні токени | що втрачаємо |
|---|---|---|---|---|
| | | | | |
| | | | | |
| | | | | |

Обране значення й **чому саме воно**: `<...>`

## 6. Рішення й межі

### Клас виправлення на кожну точку відмови

| Точка | Клас виправлення | Очікуване зрушення метрики |
|---|---|---|
| | переіндексація / промпт / набір / інше | |

> Гіпотеза записується **до** правки. Якщо після неї зрушилося не те, що ви
> передбачили, гіпотеза була неповна — і це видно.

### Що промптом не лікується взагалі

`<...>`

### Клас збоїв, який проходить усі три метрики

`<...>`

> Обов'язковий розділ. Метрики RAG сліпі до цілого класу — назвіть його й
> скажіть, чим покриваєте.

### Залишковий ризик

`<що лишається після всіх правок і чому це прийнято>`
