# Gate Policy — <ваша система>

Шаблон під довільну систему. Дев'ять рядків; порожній рядок — це не
«пропустили», а «рішення не ухвалене», і його видно на ревʼю.

---

## 1. Мандат

Діє: `<Ship it | Don't break it | інший>`. Чому саме він: `<стадія продукту,
ціна пропущеного дефекту проти ціни зупиненого релізу>`.

Що змінилося б за протилежного мандату: `<які конкретно пороги й права
змінилися б — назвіть рядки цього документа>`.

> Без цього рядка решта документа не читається: усі пороги нижче — наслідок
> мандату, а не властивість системи.

## 2. Що блокує мердж

Обов'язковий чек: `<назва чека>`. Червоніє, коли `<умова>`.

Склад блокуючого шару за критерієм: `<який критерій відбору — напр. лише
детерміновані перевірки на одну репліку>`.
Ціна прогону: `<хвилини, гроші>`.

## 3. Що НЕ йде в гейт — і чим покривається натомість

| Поріг | Чому не в гейті | Чим покрито |
|---|---|---|
| | | |
| | | |
| | | |

> Мінімум три рядки, і **покриття заповнюється першим**. «Не в гейті» без
> покриття — це не рішення, а прогалина.

## 4. Розклад прогонів

| Шар | Коли | Склад | Ціна |
|---|---|---|---|
| | | | |

## 5. Периметр вимірювальних інструментів

Суддя / зовнішні сервіси: `<де виконуються, які дані виходять за периметр>`.
Чим доведено: `<яке поле в артефакті це показує>`.

## 6. Право обходу

| Шар | Хто може обійти | Як |
|---|---|---|
| | | |

> Хоча б один шар мусить мати **порожнє** поле «хто може обійти». Якщо
> обійти можна все, гейта немає.

## 7. Що записує кожен прогін

| Поле | Навіщо |
|---|---|
| | |

> П'ять мінімум. Питання, на яке вони відповідають: «чи можна покласти цей
> прогін поруч із тим?»

## 8. Як інтегровано

Механізм: `<код повернення | інше>`.
Чого в workflow **немає**: `<continue-on-error, парсинг логу, || true>`.
Доведено: `<посилання на зелений і червоний артефакти на однаковому хеші
набору>`.

## 9. За яких умов ця політика зміниться

- `<тригер>` → `<що саме зміниться>`
- `<тригер>` → `<що саме зміниться>`

Версія: `<vN>`, `<дата>`. Попередня: `<vN-1, статус superseded, чому>`.
