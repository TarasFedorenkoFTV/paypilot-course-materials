# Заява про довіру до судді — `<ваша система>`

Документ, на підставі якого команда вирішує, що судді **дозволено**. Без
цифр у розділах 2 і 3 решта не має сили.

---

## 1. Що суддя вимірює

Рубрика `<версія>`: `<які саме властивості відповіді оцінює>`.
Модель судді: `<модель, тег>` · температура: `<...>` · промпт: `<версія>`.

Чого суддя **не** вимірює: `<...>`

> Суддя, що «оцінює якість», не вимірює нічого. Назвіть властивість.

## 2. Наскільки суддя точний

Розмічений набір: `<N>` пар, розмітка `<ким і за яким правилом>`.

| Метрика | Значення | 95% інтервал |
|---|---|---|
| узгодженість із людиною | `<...>` | `<...>` |
| хибно-позитивні | | |
| хибно-негативні | | |

> **Інтервал обов'язковий.** «80% узгодженості» на 30 прикладах і на 300 — це
> різні твердження, і рішення на них теж різні.

## 3. Де суддя не працює

Класи, на яких він систематично помиляється:

| Клас входу | Що робить суддя | Заміряно на |
|---|---|---|
| | | |
| | | |

> Порожня таблиця означає не «таких класів немає», а «ми їх не шукали».

## 4. Системна помилка, яку знайшли й виправили

Що саме: `<...>`
Як виявили: `<...>`
Що змінили — рубрику, промпт судді чи набір: `<...>`
Метрика до / після: `<...>`

## 5. Коли перекалібровувати

- при зміні `<...>`
- не рідше ніж `<...>`

> Обов'язковий пункт: **зміна моделі судді або моделі під оцінкою**. Метрики —
> властивість пари, і після заміни старі цифри недійсні.

## 6. Що дозволено на цих цифрах

| Дія | Дозволено? | Чому |
|---|---|---|
| блокувати мердж | | |
| ранжувати кандидатів | | |
| моніторити тренд | | |

> Найчастіша помилка курсу — ставити суддю в блокуючий гейт із похибкою
> ±10 п.п. Це блокувати випадково.

## 7. Вартість

Один прогін набору: `<викликів, токенів, грошей, часу>`
На місяць за поточного розкладу: `<...>`
