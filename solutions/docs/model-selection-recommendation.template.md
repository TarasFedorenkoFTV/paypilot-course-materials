# Model Selection Recommendation — <ваша система>

Одна сторінка. Питання, на яке вона відповідає: **«доведіть, що це той самий
інструмент»**.

**Відповідь одним реченням (пишіть її першою):** `<різниця> при сумі
інтервалів `<сума>`, тобто `<різницю виявлено / не виявлено>`; рекомендуємо
`<інструмент>`.`

---

## 1. Контракт інструмента

| Пункт | Значення |
|---|---|
| Модель | `<тег>` + **дата перевірки** |
| Рубрика / конфігурація | `<версія>` |
| Режим виклику | `<температура, кількість викликів>` |
| Вихід | `<які поля>` |

> Ім'я моделі — параметр із датою, не константа. Модель за тим самим тегом
> змінюється.

## 2. Два заміри поруч

| Інструмент | Метрика | 95% інтервал | Час |
|---|---|---|---|
| A | | | |
| B | | | |

### Правило інтервалів, застосоване явно

Різниця: `<X>` п.п. Сума ширин інтервалів: `<Y>` п.п.
`<X>` `<менше / більше>` `<Y>` → `<різниці не виявлено / різниця реальна>`.

> Висновок не може бути сильнішим за правило. «Не вдалося показати різницю»
> — це не «однакові».

## 3. Розклад промахів

| Інструмент | `<третина 1>` | `<третина 2>` | `<третина 3>` |
|---|---|---|---|
| | | | |

**Де не працює — класом, не переліком кейсів:** `<клас>`.

## 4. Ціна — у двох стовпчиках

| | Гроші | Секунди |
|---|---|---|
| A | | |
| B | | |

Методика заміру часу: `<як міряли>`. Оцінюйте співвідношення, не абсолют.

## 5. Рекомендація

`<переїжджати / з умовою / ні>`. Умова: `<яка>`.

### Чого цей переїзд не вирішує — три пункти
1. `<...>`
2. `<...>`
3. `<...>`

### Рядок мандату
Діє `<мандат>`. За протилежного рішення було б `<яким і чому>`.

## 6. Статус незроблених замірів

`<що не заміряно і чому — прямо. Критерій приймання, визначений ДО заміру.>`
