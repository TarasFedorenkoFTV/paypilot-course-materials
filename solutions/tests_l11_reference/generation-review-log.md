# Generation Review Log

Критерії ревʼю записані **до** генерації. Це важливо: критерій, придуманий
після того, як побачив вивід, — це переказ виводу, а не критерій.

## Критерії (зафіксовані 2026-09-02, до першого запуску асистента)

| № | Критерій | Чому |
|---|---|---|
| C1 | тест перевіряє **рішення** в коді, а не форму повернення | `hasattr` не ловить жодного дефекту |
| C2 | є хоч один тест на **межу** (`<=` проти `<`) | там живуть off-by-one |
| C3 | очікування — конкретне значення або відношення, не `> 0` | `> 0` проходить для 99% мутантів |
| C4 | тест падає хоч на одному мутанті зі списку | інакше він нічого не перевіряє |
| C5 | назва каже, **що** ламається, не який метод викликано | назва — перше, що читають на червоному білді |
| C6 | тест не дублює інший тест по суті | два тести на одне рішення коштують удвічі |

## Журнал: рішення по кожному згенерованому тесту

Генерація: **2 хв**. Ревʼю: **26 хв** (заміряно). Разом 28 хв проти 38 хв
на ручний набір.

| Тест | Вердикт | Критерій | Причина | хв |
|---|---|---|---|---|
| `test_quote_returns_an_object_with_all_fields` | **відхилено** | C1, C4 | перевіряє наявність полів. Жоден мутант не видаляє поле — тест не падає ніколи | 2 |
| `test_quote_as_dict_is_serialisable` | **відхилено** | C1, C4 | те саме про `as_dict`: форма, не поведінка | 1 |
| `test_basic_conversion_produces_a_positive_amount` | **відхилено** | C3 | `> 0` істинне для 11 з 12 мутантів. Проходить навіть коли спред додається замість вирахування | 3 |
| `test_mid_rate_is_positive` | **відхилено** | C3, C6 | `> 0` і дублює попередній | 1 |
| `test_same_currency_conversion_has_rate_one` | **прийнято** | — | справжнє рішення (EUR→EUR = 1.0) із конкретним очікуванням | 2 |
| `test_larger_amount_gives_larger_result` | **прийнято з приміткою** | C3 | відношення, не абсолют — слабко, але ловить знак. Лишений як дешева димова перевірка | 3 |
| `test_tier_is_echoed_back` | **переписано** | C1 | перевіряв, що поле `tier` повертається як передано — тавтологія. Переписаний у `test_each_tier_gets_its_own_spread_and_the_amounts_differ`, який **вбиває M08 і M12** | 9 |
| `test_transfer_fee_returns_a_total` | **відхилено** | C3, C4 | `> 0` для комісії. M07 (втрачена фіксована частина) виживає | 2 |
| `test_unknown_currency_raises` | **прийнято — і знайшов дефект** | — | падав на справжній невідповідності: `mid_rate` кидав `KeyError`, а решта модуля обіцяє `ValueError`. Виправлено в `app/engines/fx.py` | 3 |
| `test_unknown_transfer_type_raises` | **прийнято** | — | конкретний тип винятку на конкретному вході | — |

**Разом:** 4 прийнято, 5 відхилено, 1 переписано.

## Найдорожчий рядок цього журналу

`test_unknown_currency_raises` — **єдиний тест, який знайшов справжній дефект
у стенді**, і його написав асистент, не я. Мій ручний набір цього не побачив:
я тестував правила тарифу й межі, а не типи винятків на поганих входах.

Це і є роль асистента, знайдена вимірюванням, а не заявлена наперед:
**широке дешеве покриття поганих входів**, де людина зосереджується на
рішеннях домену.
